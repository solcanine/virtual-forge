## SQLite database (generated locally — see `.gitignore`)
DATABASE_URL="file:./data/dev.db"

# Optional: enable LLM tool-calling planner (otherwise offline heuristic runs)
OPENAI_API_KEY=

# Optional override (default gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
